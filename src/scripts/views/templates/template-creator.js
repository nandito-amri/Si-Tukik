import firebaseConfig from '../../globals/firebase-config';

const createPagination = () => `
    
`;

const createPatroliTable = (sarang) => `

`;

const createInkubasiTable = (sarang) => `

`;

const createRilisTable = (perilisasn) => {

};

export { createPatroliTable, createInkubasiTable, createRilisTable };
